interface:
  display_name: "PingPong"
  short_description: "Codex builds, Claude independently critiques"
  default_prompt: "Use PingPong to implement the task after the invocation as the authoritative scope. Use Claude Sonnet as the independent critic by default, verify findings, fix valid issues, rerun deterministic gates, and stop only when the required review passes or USER_REQUIRED is reached. Never invent missing domain semantics."

policy:
  allow_implicit_invocation: false
